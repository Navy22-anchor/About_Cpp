# review Question

1. 
2. >>
3. 
```cpp
cout << "정수값을 입력하시오 :";
cin >> number;
```
4. \n 은 개행문자로 문자열 사이에 쓰이며 \n를 기준으로 줄바꿈이 된다.
5. 
6.
7.
```cpp
cout << "안녕하세요, " << first_name << "!\n";
```
8. 객체는 메모리의 공간을 의미한다.
9. 리터럴은 문자열을 의미한다.
10.
11. 변수란 객체에 이름을 붙인 것이다.
12. char(1byte), int(4bytes), double(8bytes)
13. byte
14. =는 , ==은 값이 같은지 확인하는 것
15. 정의란 값을 변수에 할당하는 행위를 말한다.
16. 초기화는 어떤 변수에 처음 할당하는 값이다. 대입은 이미 할당된 값이 존재한다.
17. 문자열 연결은 다른 객체에 저장된 문자열을 연결한다는 의미이다. c++에서는 +연산자로 할 수 있다.
18. 
19.
20.
21. 타입 안전성이란 변수에 지정된 타입이 바뀌어 범위내에 존재할경우 타입 안전성이 있다고 하고 아닐경우 없다고 한다.
22. 축소변환이 일어난다. 즉, double과 같이 큰 사이즈를 작은 사이즈인 int에 할당하여 발생하는 문제이다.
23. 목록 초기화 이다. int a {0} 으로 작성할 수 있다.
